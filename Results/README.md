Results description

This folder contains the aggregate result tables used for the manuscript figures.

The folder does not contain raw matrices, model checkpoints, temporary scores, or training cache files.

The result tables report F1, Precision, Recall, AUPRC, and ROC AUC. New experiment results should be written to a separate results folder so that these supplied tables are not overwritten.
